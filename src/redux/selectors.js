export const todosSelector = state => state.todos.todos;
export const loadingTodosSelector = state => state.todos.loading;
export const photosSelector = state =>state.photos.photos;
export const loadingPhotosSelector = state => state.photos.loading;